<div class="question-screen">
  <h1><%= @category.capitalize %></h1>
  <h2><%= @question %></h2>
  <div class="question-options">
    <% @options.each do |option| %>
      <% if option == @correct_answer %>
        <a class="option" href="/game/<%= @category %>/correct"><%= option %></a>
      <% else %>
        <a class="option" href="/game/<%= @category %>/incorrect"><%= option %></a>
      <% end %>
    <% end %>
  </div>
  <a href="/game/<%=@category%>">
    <!-- Pick another <%= @category %> question -->
    <img class="reroll" src="/images/reroll-icon.svg" alt="reroll-question">
  </a>
  <a href="/game">Pick another category</a>
</div>
